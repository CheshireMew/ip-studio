# Archived monolithic entrypoint

This directory preserves the final single-Skill IP Studio entrypoint and README from before the suite was split. It is historical material, not an active Skill and not an installation target.

The active entrypoints are now `skills/ip-studio`, `skills/ip-visuals`, `skills/motion-studio`, and `skills/pet-studio`.
